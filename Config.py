# with functions that save and load experiment configurations - as JSON
# functions that check which experiment configs are available and which participants already completed each experiment
# one function that transforms the experiment config to an actual trial sequence when running a participant
# and functions that store / update the state of a running experiment - also as JSON (or does this go to Exp.py?)
